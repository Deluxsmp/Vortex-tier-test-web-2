# Vortex Tier V17

Updated the player skin rendering to use the Minecraft 3D body render directly. The character is cropped at the waist so the head, arms and torso remain visible, while the render's native 3/4 perspective is preserved instead of adding a flat CSS rotation.

Open `rankings.html` in a browser. Internet access is required for the skin renders and linked mode icons.


V18 skin rendering: uses the MC API BUST render for a true Minecraft bust (head, torso, both arms) rather than CSS-cropping a full-body image.

Combat rank system: #1 = Combat Grandmaster, #2-#3 = Combat Master, #4+ = Combat Ace.

V26 visual update: Overall player rows now use a compact waist-level bust crop, a slanted accent behind each player, larger placement numbers/region badges, larger tier icons and tier labels, and stronger top mode icons.
